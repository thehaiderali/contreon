import React from 'react'

const CreatorPagePrivate = ({creatorUrl,user}) => {
  return (

    <div>CreatorPagePrivate :{creatorUrl} for user : {user.fullName}</div>
  )
}

export default CreatorPagePrivate